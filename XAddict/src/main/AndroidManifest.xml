<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reduce_abuse.xaddict">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MethActivity"></activity>
        <activity android:name=".HeroinActivity" />
        <activity android:name=".OpioidsActivity" />
        <activity android:name=".SyntheticsActivity" />
        <activity android:name=".CocaineActivity" />
        <activity android:name=".MarijuanaActivity" />
        <activity android:name=".DrugsListActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".PersonalNotesActivity" />
        <activity android:name=".HomepageActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>